//same charcters, same frequency 
#include <iostream>
using namespace std ;
void anagram(string &s1,string &s1)
{
  vector<int> freq(26,0);
  if(s1.length()!=s2.length())
    return false ;
  for(char x : s1)
  
    freq[x-'a']++ ;
  for( char x :s2)
    freq[x-'a']--;
  for(int count :freq )
  {
    if(freq[i])
      return false
  }
  return true ;
  

}