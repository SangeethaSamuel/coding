#include <stdio.h>
#include <vector>
#include <bits/stdc++.h>
using namespace std ;
void morethanhalf(vector<int> arr , int n)
{
  sort(arr.begin(),arr.end());
  int candidate =arr[n/2]; //since occuring more than half no of times 
}